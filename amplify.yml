version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - npm ci
        build:
          commands:
            - npm run build
      cache:
        paths:
          - node_modules/**/*
    appRoot: annotation_ui
